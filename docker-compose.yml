services:
  tradebot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - tradebot-data:/app/data
      - tradebot-logs:/app/logs

volumes:
  tradebot-data:
  tradebot-logs:
